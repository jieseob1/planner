apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nowline
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "1m"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "5"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "30"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts: [goalstotoday.com, www.goalstotoday.com]
      secretName: nowline-tls
  rules:
    - host: goalstotoday.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nowline-frontend
                port:
                  name: http
    - host: www.goalstotoday.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nowline-frontend
                port:
                  name: http
