[SERVICE]
    Flush 1
    Grace 1
    Log_Level error
[INPUT]
    Name stdin
    Tag nowline.backend
[FILTER]
    Name lua
    Match nowline.*
    script /fluent-bit/etc/nowline/sanitize.lua
    call sanitize
    protected_mode true
[OUTPUT]
    Name stdout
    Match *
    Format json_lines
